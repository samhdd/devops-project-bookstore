# Environment Configuration for Images
# Add these to your .env file (create if it doesn't exist)

# Image storage configuration
IMAGE_STORAGE_TYPE=local  # Options: 'local', 'cloudinary', 's3'
IMAGE_BASE_URL=http://localhost:5000/api/images

# For cloud storage (when using cloud providers)
# CLOUDINARY_URL=cloudinary://your-key:your-secret@your-cloud-name
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
